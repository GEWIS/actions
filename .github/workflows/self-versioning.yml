name: Versioning of the actions repository

on:
  push:
    branches:
      - main

jobs:
  versioning:
    uses: ./.github/workflows/versioning.yml
    permissions:
      contents: write
      pull-requests: write
      issues: write

